{
  "name": "tujjar-almawashi-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Server-side (Admin SDK) operations that a client-only Firebase app cannot safely do on its own: resetting a forgotten password via phone/SMS OTP, and permanently deleting a user's Firebase Auth account.",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.3.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
